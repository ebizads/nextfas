export type ColumnType = {
  name: string
  filtered: boolean
}

export type RowType = {
  id: number
  serial_no: string
  bar_code: string
  type: string
  category: string
  name: string
  description: string
  owner: string
  added_date: string
}
